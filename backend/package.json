{
  "name": "rmit-vertex-dynamics-backend",
  "version": "2.0.0",
  "private": true,
  "description": "Local Express service: seam descriptor ingestion, validation, path planning, motion-only ABB RAPID generation and job records",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test \"test/*.test.js\"",
    "bench": "node scripts/benchmark.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
