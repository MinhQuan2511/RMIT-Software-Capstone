# Copy to frontend/.env.local if the backend is not on the default address.
# NEXT_PUBLIC_* values are compiled into the browser bundle: never put a secret here.
NEXT_PUBLIC_API_URL=http://127.0.0.1:5000/api
